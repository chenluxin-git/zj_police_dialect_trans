# ---- 宝塔演示部署的传输产物与暂存目录（由源码+构建再生，不入库）----
bt_file/
record_demo_*.zip
record_frontend_*.zip
dist-record/
dist-stack/
dist-offline/

# ---- compose 外挂的运行日志（docker-compose.yml 挂到 ./logs 的宿主侧落点）----
logs/

# ---- 部署机本地自签证书（deploy/README 附 openssl 命令按机生成）----
deploy/certs/

# ---- 演练机本地 compose 端口避让覆盖（80 被同机其他项目占用时用）----
docker-compose.override.yml

# ---- 系统杂项 ----
.DS_Store
Thumbs.db