{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build && node scripts/check-es-target.cjs && node scripts/check-menu-routes.mjs",
    "build:record-demo": "vue-tsc -b && vite build --mode record-demo && node scripts/check-es-target.cjs",
    "check:es-target": "node scripts/check-es-target.cjs",
    "check:polyfill": "node scripts/check-polyfill.cjs",
    "check:menu": "node scripts/check-menu-routes.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.2",
    "axios": "^1.20.0",
    "element-plus": "^2.14.6",
    "pinia": "^4.0.3",
    "vue": "^3.5.32",
    "vue-router": "^5.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@vitejs/plugin-vue": "^6.0.5",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.4",
    "vue-tsc": "^3.2.6"
  }
}
