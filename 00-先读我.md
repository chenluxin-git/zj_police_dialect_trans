# 打包说明（拿到本目录后先看这个）

本目录是**浙警智治方言语料采集平台**的源码交付包，用于在另一台机器上构建**离线交付包**。
（并入主仓库后本文件兼作**离线包打包指引**，脚本就在仓库根 `scripts/`。）

## 一、你要做的事

```bash
cd <仓库根目录>

# 构建镜像并导出离线包（后端 + 前端）
bash scripts/export-offline.sh

# 内网用 MySQL 时，一并导出 MySQL 镜像
bash scripts/export-offline.sh --with-db
```

产出 `dist-offline/zjpdt-offline-<时间戳>.tar.gz`，拷到内网服务器即可。

> **前提**：这台机器要能**上外网**且 **docker 守护进程在运行**。
> 因为 web 镜像是多阶段构建（构建期要 `npm ci`），内网构建不了，必须在联网机构建后 `docker save`。

## 二、可选：本机验证（不需要 docker）

```bash
# 前端（需联网装依赖）
cd web
npm ci --registry=https://registry.npmmirror.com
npm run build          # 含 vue-tsc 类型检查 + Chrome80 产物体检 + 菜单路由自检

# 后端（任意 Python 3.10+）
cd ../server
python -m venv .venv
.venv/bin/pip install -r requirements.txt     # Windows: .venv\Scripts\pip
.venv/bin/python -m pytest -q                 # 应有 156 passed + 4 skipped（设 TEST_MYSQL_URL 时 160 passed）
.venv/bin/python -m uvicorn app.main:app --port 8000
```

> 本包**未含 `server/.venv`**（Windows 本地虚拟环境，跨机器无意义且体积大），按上面命令重建即可。

## 三、本包包含什么

| 目录 | 说明 |
|---|---|
| `server/` | FastAPI 后端（含 Dockerfile、requirements、环境变量模板） |
| `web/` | Vue 3 前端（含 Dockerfile、package-lock、构建脚本） |
| `deploy/` | nginx 上架版配置、证书目录 |
| `scripts/` | **离线包导出/导入、日志收集、联调预检**脚本 |
| `docs/` | 部署与排障文档（见下） |
| `dome/` | 静态设计稿（存档，不参与构建） |
| `docker-compose.yml` | 双容器编排（backend + web，可选 mysql profile） |

### 交付 zip 包里已排除的（主仓库内仍在，此清单仅描述打包裁剪）

`server/.venv`、`server/data`、`server/logs`、`server/audio_storage`（演示 WAV，1.1GB）、
`server/tests`、`web/node_modules`、`web/dist`。

> 其中 **`web/node_modules` 必须排除**：`web/.dockerignore` 里有它，
> 若打进构建上下文会拖慢构建甚至出错。让 docker 自己 `npm ci`。

## 四、必读文档

| 文档 | 什么时候看 |
|---|---|
| `docs/offline-deploy.md` | **内网部署全流程**（导出→导入→配置→启动→验证） |
| `docs/diagnose-playbook.md` | **出问题时**：现象 → 该看哪个日志字段 |
| `docs/deploy-zhijing-onboarding.md` | 浙警智治联调步骤（认证/审计/组织同步） |

## 五、出问题把日志回传

```bash
# 内网服务器上（部署目录）
bash scripts/collect-logs.sh --url https://<你的地址>
# 产出 logpack-<时间戳>.tar.gz，回传即可
```

日志含**启动自检报告**、请求日志（带 trace_id）、**前端错误上报**、nginx 日志、
docker 状态、脱敏后的配置快照。

---

## 六、上线前两件要确认的事

1. **域定性**：当前按**用户域**（`lxrdl.gat.zj` / `41.188.255.179`）配置。
   若要接**统一用户/部门同步**，按平台规范应落**数据域**，服务地址与认证方式都要换，
   且部署前需先申请云桌面/堡垒机。详见 `docs/offline-deploy.md` §9。

2. **智慧运维四件套尚未接入**（Matomo 埋点 / SLS-AIOS 日志采集 / GAPM 探针 / 浙警智评悬浮球）。
   不影响内网联调，但**上架验收前必须补**。
